{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "neuzz build",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
                "${workspaceFolder}/neuzz.c",
                "-o",
                "${workspaceFolder}/neuzz"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "调试器生成的任务。"
        }
    ],
    "version": "2.0.0"
}